# Root BUILD file for rules_codex

load("//codex:defs.bzl", "local_auth_flag")

local_auth_flag(
    name = "local_auth",
    build_setting_default = False,
    visibility = ["//visibility:public"],
)
